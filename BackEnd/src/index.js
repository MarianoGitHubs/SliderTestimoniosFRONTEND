import app from './app.js';

// Se inicia el servidor web en el puerto 3000. Esto significa que la aplicación Express estará escuchando las solicitudes entrantes en ese puerto.
app.listen(3000);

console.log('Server on port', 3000);